let x;
x = Math.trunc(101 * Math.random());
console.log(x);

// Не совсем верно, т.к. по заданию нужно возвращать рандомные числа от 0 до 100 (включительно). В вашем случае 100 не включен в диапазон возможных значений. Выше исправила на верный вариант